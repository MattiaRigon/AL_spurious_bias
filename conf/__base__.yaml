defaults:
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - _self_

seed: 42
wandb:
  mode: online
  project: manhattan
  entity: yjng
  tags: null

hydra:
  job:
    chdir: True
    env_set:
      USE_CUDA: true
